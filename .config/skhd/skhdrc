# Changing Window Focus
alt - j : yabai -m window --focus south
alt - k : yabai -m window --focus north 
alt - h : yabai -m window --focus west
alt - l : yabai -m window --focus east 

# Change display focus
alt - w: yabai -m display --focus west
alt - e: yabai -m display --focus east

# Modify layout
shift + alt - r: yabai -m space --rotate 270 # rotate layout CW
shift + alt - y: yabai -m space --mirror y-axis # flip along y-axis
shift + alt - x: yabai -m space --mirror x-axis # flip along x-axis
shift + alt - f: yabai -m window --toggle float --grid 4:4:1:1:2:2 # toggle floating

# Modifying window size
alt - f: yabai -m window --toggle zoom-fullscreen # Fullscreen window
shift + alt - e: yabai -m space --balance # balance window sizes

# Move Windows

## Swap windows
shift + alt - j: yabai -m window --swap south
shift + alt - k: yabai -m window --swap north
shift + alt - h: yabai -m window --swap west
shift + alt - l : yabai -m window --swap east

## Move window and split
ctrl + alt - j: yabai -m window --warp south
ctrl + alt - k: yabai -m window --warp north
ctrl + alt - h: yabai -m window --warp west
ctrl + alt - l : yabai -m window --warp east

## Move window to display
shift + alt - w: yabai -m window --display west; yabai -m display --focus west;
shift + alt - e: yabai -m window --display east; yabai -m display --focus east;

## Move window to prev and next space
shift + alt - p: yabai -m window --space prev;
shift + alt - n: yabai -m window --space next;

## Move window to space #
cmd + shift - 1 : yabai -m window --space 1;
cmd + shift - 2 : yabai -m window --space 2;
cmd + shift - 3 : yabai -m window --space 3;
cmd + shift - 4 : yabai -m window --space 4;
cmd + shift - 5 : yabai -m window --space 5;
cmd + shift - 6 : yabai -m window --space 6;
cmd + shift - 7 : yabai -m window --space 7;
cmd + shift - 8 : yabai -m window --space 8;
cmd + shift - 9 : yabai -m window --space 9;
cmd + shift - 0 : yabai -m window --space 10;

## Start/stop yabai
ctrl + alt - q: brew services stop yabai
ctrl + alt - s: brew services start yabai
